---
name: No Deferring Directive
description: NEVER defer work items. Complete all identified fixes in the current session. No "later", no "next session", no "deferred".
type: feedback
---

Never defer work. When fixes are identified, execute them all in the current session.

**Why:** The operator explicitly stated "we cannot defer anything" and "I never want to hear this word again." Deferring signals mediocre work ethic and creates technical debt that compounds. The operator runs multiple agents on different workstreams — each agent is expected to complete its scope fully.

**How to apply:**
- When a P0/P1/P2 fix list exists, execute ALL of them. Don't split into "now" and "later."
- If a fix is complex (e.g., needs method generation), that's not a reason to defer — it's a reason to plan and execute properly.
- A second-opinion tool's insights are fuel for intellect, not code to copy. Use them to prevent hallucination but write everything yourself.
- The only valid reason to stop is if the operator explicitly redirects to different work.
